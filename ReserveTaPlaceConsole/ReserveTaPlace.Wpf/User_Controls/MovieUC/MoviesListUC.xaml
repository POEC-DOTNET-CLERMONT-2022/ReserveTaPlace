<UserControl x:Class="ReserveTaPlace.Wpf.User_Controls.MovieUC.MoviesListUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             xmlns:local="clr-namespace:ReserveTaPlace.Wpf.User_Controls"
             xmlns:User_ControlsMovieUC="clr-namespace:ReserveTaPlace.Wpf.User_Controls.MovieUC"
             xmlns:User_Controls="clr-namespace:ReserveTaPlace.Wpf.User_Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             x:Name="root" Loaded="root_Loaded"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Column="0" x:Name="LBMovies" SelectionChanged="LBMovies_SelectionChanged" SelectedItem="{Binding CurrentMovie}" DataContext="{Binding .,ElementName=root}" ItemsSource="{Binding Movies}">
            <!--<ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="IsSelected" Value="True"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>-->
            <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                    <WrapPanel  HorizontalAlignment="Left">

                    </WrapPanel>
                </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Width="150" >
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="160" />
                                <RowDefinition Height="35" />
                            </Grid.RowDefinitions>
                            <Image Source="{Binding Poster}"  Stretch="Uniform" Margin="0 20 0 0"/>
                            <StackPanel Grid.Row="1" Margin="8 0 8 0">
                                <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" Text="{Binding Title}" />
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel HorizontalAlignment="Stretch" x:Name="SPMovieDetails" Grid.Column="1" VerticalAlignment="Top" Margin="10 0 10 0">
            <User_ControlsMovieUC:MovieDetailsUC CurrentMovie="{Binding SelectedItem, ElementName=LBMovies}"/>
            <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 10 0 0" x:Name="BTNPutOnScreen" Click="BTNPutOnScreen_Click" >Mettre le film à l'affiche</Button>
        </StackPanel>
        <User_ControlsMovieUC:PutMovieOnScreenUC Grid.Column="0" x:Name="UCPutOnScreen" Visibility="Collapsed"/>
    </Grid>
</UserControl>
