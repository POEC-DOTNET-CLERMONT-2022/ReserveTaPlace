<UserControl x:Class="ReserveTaPlace.Wpf.User_Controls.UserPageUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}" 
             xmlns:local="clr-namespace:ReserveTaPlace.Wpf.User_Controls"
             xmlns:User_Controls="clr-namespace:ReserveTaPlace.Wpf.User_Controls"
             xmlns:UserUC="clr-namespace:ReserveTaPlace.Wpf.User_Controls.UserUC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <User_Controls:MainMenuUC Grid.Column="0" x:Name="MainMenuUC"></User_Controls:MainMenuUC>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <WrapPanel  HorizontalAlignment="Left">
                <TextBox materialDesign:HintAssist.Hint="User@Mail" x:Name="TBUserName" Margin="0,0,10,0" Width="150"></TextBox>
                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Rechercher" Background="{DynamicResource MaterialDesignTextFieldBoxBackground}">
                    <materialDesign:PackIcon Kind="AccountSearch" />
                </Button>
            </WrapPanel>
            <UserUC:UsersListUC x:Name="UCUsersList" Users="{Binding Users}" Grid.Row="1" />
            <UserUC:UsersPagerUC Grid.Row="2" GoPreviousPage="UsersPagerUC_GoPreviousPage" GoNextPage="UsersPagerUC_GoNextPage"/>
            <Button  Grid.Row="0" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Ajouter" Background="{DynamicResource MaterialDesignTextFieldBoxBackground}">
                <materialDesign:PackIcon Kind="AccountPlus" />
            </Button>
            <UserUC:AddUserUC x:Name="UCAddUser" UserViewModel="{Binding UserViewModel}" HorizontalAlignment="Left" Grid.Row="3"/>
        </Grid>
    </Grid>
</UserControl>
    
